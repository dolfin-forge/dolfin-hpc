// This code conforms with the UFC specification version 2018.1.0
// and was automatically generated by FFC version 2019.1.0.
//
// This code was generated with the following parameters:
//

//  add_tabulate_tensor_timing:     False
//  convert_exceptions_to_warnings: False
//  cpp_optimize:                   True
//  cpp_optimize_flags:             '-O2'
//  epsilon:                        1e-14
//  error_control:                  False
//  external_include_dirs:          ''
//  external_includes:              ''
//  external_libraries:             ''
//  external_library_dirs:          ''
//  form_postfix:                   True
//  format:                         'ufc'
//  generate_dummy_tabulate_tensor: False
//  max_signature_length:           0
//  no-evaluate_basis:              False
//  no-evaluate_basis_derivatives:  False
//  optimize:                       True
//  precision:                      None
//  quadrature_degree:              None
//  quadrature_rule:                None
//  representation:                 'auto'
//  split:                          True

#include "ffc_Discontinuous_Lagrange_0_2d.h"

ffc_discontinuous_lagrange_0_2d_finite_element_0::ffc_discontinuous_lagrange_0_2d_finite_element_0() : ufc::finite_element()
{
    // Do nothing
}

ffc_discontinuous_lagrange_0_2d_finite_element_0::~ffc_discontinuous_lagrange_0_2d_finite_element_0()
{
    // Do nothing
}

const char * ffc_discontinuous_lagrange_0_2d_finite_element_0::signature() const
{
    return "FiniteElement('Discontinuous Lagrange', triangle, 0)";
}

ufc::shape ffc_discontinuous_lagrange_0_2d_finite_element_0::cell_shape() const
{
    return ufc::shape::triangle;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::topological_dimension() const
{
    return 2;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::geometric_dimension() const
{
    return 2;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::space_dimension() const
{
    return 1;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::value_rank() const
{
    return 0;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::value_dimension(std::size_t i) const
{
    return 1;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::value_size() const
{
    return 1;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::reference_value_rank() const
{
    return 0;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::reference_value_dimension(std::size_t i) const
{
    return 1;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::reference_value_size() const
{
    return 1;
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::degree() const
{
    return 0;
}

const char * ffc_discontinuous_lagrange_0_2d_finite_element_0::family() const
{
    return "Discontinuous Lagrange";
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::evaluate_reference_basis(double * reference_values,
                                             std::size_t num_points,
                                             const double * X) const
{
    static const double coefficients0[1][1] = { { 1.0 } };
    for (std::size_t k = 0; k < num_points; ++k)
        reference_values[k] = 0.0;
    for (std::size_t ip = 0; ip < num_points; ++ip)
    {
        // Compute basisvalues for each relevant embedded degree
        double basisvalues0[1] = {};
        basisvalues0[0] = 1.0;
        // Accumulate products of coefficients and basisvalues
        reference_values[ip] += coefficients0[0][0] * basisvalues0[0];
    }
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::evaluate_reference_basis_derivatives(double * reference_values,
                                                         std::size_t order,
                                                         std::size_t num_points,
                                                         const double * X) const
{
    if (order == 0)
    {
        evaluate_reference_basis(reference_values, num_points, X);
        return;
    }
    const std::size_t num_derivatives = std::pow(2, order);
    std::fill_n(reference_values, num_points * num_derivatives, 0.0);
    if (order > 0)
        return;
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::transform_reference_basis_derivatives(double * values,
                                                          std::size_t order,
                                                          std::size_t num_points,
                                                          const double * reference_values,
                                                          const double * X,
                                                          const double * J,
                                                          const double * detJ,
                                                          const double * K,
                                                          int cell_orientation) const
{
    std::fill_n(values, num_points, 0.0);
    const std::size_t reference_offsets[1] = {};
    const std::size_t physical_offsets[1] = {};
    for (std::size_t ip = 0; ip < num_points; ++ip)
    {
        double transform[1][1];
        for (std::size_t r = 0; r < 1; ++r)
            for (std::size_t s = 0; s < 1; ++s)
                transform[r][s] = 1.0;
        for (std::size_t d = 0; d < 1; ++d)
        {
            for (std::size_t s = 0; s < 1; ++s)
            {
                for (std::size_t i = 0; i < 1; ++i)
                {
                    // Using affine transform to map values back to the physical element.
                    const double mapped_value = reference_values[ip + d + s + reference_offsets[d]];
                    // Mapping derivatives back to the physical element
                    for (std::size_t r = 0; r < 1; ++r)
                        values[ip + d + r + (physical_offsets[d] + i)] += transform[r][s] * mapped_value;
                }
            }
        }
    }
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::evaluate_basis(std::size_t i,
                                   double * values,
                                   const double * x,
                                   const double * coordinate_dofs,
                                   int cell_orientation,
                                   const ufc::coordinate_mapping * cm
                                   ) const
{
    double X[2] = {};
    double J[4];
    double detJ;
    double K[4];
    if (cm)
    {
        cm->compute_reference_geometry(X, J, &detJ, K, 1, x, coordinate_dofs, cell_orientation);
    }
    else
    {
        compute_jacobian_triangle_2d(J, coordinate_dofs);
        compute_jacobian_inverse_triangle_2d(K, detJ, J);
    }
    // Evaluate basis on reference element
    double ref_values[1];
    evaluate_reference_basis(ref_values, 1, X);
    // Push forward
    double physical_values[1];
    transform_reference_basis_derivatives(physical_values, 0, 1, ref_values, X, J, &detJ, K, cell_orientation);
    for (std::size_t k = 0; k < 1; ++k)
        values[k] = physical_values[i + k];
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::evaluate_basis_all(double * values,
                                       const double * x,
                                       const double * coordinate_dofs,
                                       int cell_orientation,
                                       const ufc::coordinate_mapping * cm
                                       ) const
{
    // Element is constant, calling evaluate_basis.
    evaluate_basis(0, values, x, coordinate_dofs, cell_orientation);
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::evaluate_basis_derivatives(std::size_t i,
                                               std::size_t n,
                                               double * values,
                                               const double * x,
                                               const double * coordinate_dofs,
                                               int cell_orientation,
                                               const ufc::coordinate_mapping * cm
                                               ) const
{
    std::size_t num_derivatives = std::pow(2, n);
    std::fill_n(values, num_derivatives, 0.0);
    // Call evaluate_basis_all if order of derivatives is equal to zero.
    if (n == 0)
    {
        evaluate_basis(i, values, x, coordinate_dofs, cell_orientation);
        return;
    }
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::evaluate_basis_derivatives_all(std::size_t n,
                                                   double * values,
                                                   const double * x,
                                                   const double * coordinate_dofs,
                                                   int cell_orientation,
                                                   const ufc::coordinate_mapping * cm
                                                   ) const
{
    // Element is constant, calling evaluate_basis_derivatives.
    evaluate_basis_derivatives(0, n, values, x, coordinate_dofs, cell_orientation);
}

double ffc_discontinuous_lagrange_0_2d_finite_element_0::evaluate_dof(std::size_t i,
                                   const ufc::function& f,
                                   const double * coordinate_dofs,
                                   int cell_orientation,
                                   const ufc::cell& c,
                                   const ufc::coordinate_mapping * cm
                                   ) const
{
    // Declare variables for result of evaluation
    double vals[1];
    // Declare variable for physical coordinates
    double y[2];
    switch (i)
    {
    case 0:
        {
            y[0] = 0.3333333333333334 * coordinate_dofs[0] + 0.3333333333333333 * coordinate_dofs[2] + 0.3333333333333333 * coordinate_dofs[4];
            y[1] = 0.3333333333333334 * coordinate_dofs[1] + 0.3333333333333333 * coordinate_dofs[3] + 0.3333333333333333 * coordinate_dofs[5];
            f.evaluate(vals, y, c);
            return vals[0];
        }
        break;
    }
    return 0.0;
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::evaluate_dofs(double * values,
                                  const ufc::function& f,
                                  const double * coordinate_dofs,
                                  int cell_orientation,
                                  const ufc::cell& c,
                                  const ufc::coordinate_mapping * cm
                                  ) const
{
    // Declare variables for result of evaluation
    double vals[1];
    // Declare variable for physical coordinates
    double y[2];
    y[0] = 0.3333333333333334 * coordinate_dofs[0] + 0.3333333333333333 * coordinate_dofs[2] + 0.3333333333333333 * coordinate_dofs[4];
    y[1] = 0.3333333333333334 * coordinate_dofs[1] + 0.3333333333333333 * coordinate_dofs[3] + 0.3333333333333333 * coordinate_dofs[5];
    f.evaluate(vals, y, c);
    values[0] = vals[0];
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::interpolate_vertex_values(double * vertex_values,
                                              const double * dof_values,
                                              const double * coordinate_dofs,
                                              int cell_orientation,
                                              const ufc::coordinate_mapping * cm
                                              ) const
{
    // Evaluate function and change variables
    vertex_values[0] = dof_values[0];
    vertex_values[1] = dof_values[0];
    vertex_values[2] = dof_values[0];
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::tabulate_dof_coordinates(double * dof_coordinates,
                                             const double * coordinate_dofs,
                                             const ufc::coordinate_mapping * cm
                                             ) const
{
    dof_coordinates[0] = 0.3333333333333334 * coordinate_dofs[0] + 0.3333333333333333 * coordinate_dofs[2] + 0.3333333333333333 * coordinate_dofs[4];
    dof_coordinates[1] = 0.3333333333333334 * coordinate_dofs[1] + 0.3333333333333333 * coordinate_dofs[3] + 0.3333333333333333 * coordinate_dofs[5];
}

void ffc_discontinuous_lagrange_0_2d_finite_element_0::tabulate_reference_dof_coordinates(double * reference_dof_coordinates) const
{
    static const double dof_X[2] = { 0.3333333333333333, 0.3333333333333333 };
    std::copy_n(dof_X, 2, reference_dof_coordinates);
}

std::size_t ffc_discontinuous_lagrange_0_2d_finite_element_0::num_sub_elements() const
{
    return 0;
}

ufc::finite_element * ffc_discontinuous_lagrange_0_2d_finite_element_0::create_sub_element(std::size_t i) const
{
    return nullptr;
}

ufc::finite_element * ffc_discontinuous_lagrange_0_2d_finite_element_0::create() const
{
    return new ffc_discontinuous_lagrange_0_2d_finite_element_0();
}


ffc_discontinuous_lagrange_0_2d_dofmap_0::ffc_discontinuous_lagrange_0_2d_dofmap_0() : ufc::dofmap()
{
    // Do nothing
}

ffc_discontinuous_lagrange_0_2d_dofmap_0::~ffc_discontinuous_lagrange_0_2d_dofmap_0()
{
    // Do nothing
}

const char * ffc_discontinuous_lagrange_0_2d_dofmap_0::signature() const
{
    return "FFC dofmap for FiniteElement('Discontinuous Lagrange', triangle, 0)";
}

bool ffc_discontinuous_lagrange_0_2d_dofmap_0::needs_mesh_entities(std::size_t d) const
{
    static const bool return_values[3] = { false, false, true };
    if (d >= 3)
        return false;
    return return_values[d];
}

std::size_t ffc_discontinuous_lagrange_0_2d_dofmap_0::topological_dimension() const
{
    return 2;
}

std::size_t ffc_discontinuous_lagrange_0_2d_dofmap_0::global_dimension(const std::vector<std::size_t>&
                                            num_global_entities) const
{
    return num_global_entities[2];
}

std::size_t ffc_discontinuous_lagrange_0_2d_dofmap_0::num_global_support_dofs() const
{
    return 0;
}

std::size_t ffc_discontinuous_lagrange_0_2d_dofmap_0::num_element_support_dofs() const
{
    return 1;
}

std::size_t ffc_discontinuous_lagrange_0_2d_dofmap_0::num_element_dofs() const
{
    return 1;
}

std::size_t ffc_discontinuous_lagrange_0_2d_dofmap_0::num_facet_dofs() const
{
    return 0;
}

std::size_t ffc_discontinuous_lagrange_0_2d_dofmap_0::num_entity_dofs(std::size_t d) const
{
    static const std::size_t return_values[3] = { 0, 0, 1 };
    if (d >= 3)
        return 0;
    return return_values[d];
}

std::size_t ffc_discontinuous_lagrange_0_2d_dofmap_0::num_entity_closure_dofs(std::size_t d) const
{
    static const std::size_t return_values[3] = { 0, 0, 1 };
    if (d >= 3)
        return 0;
    return return_values[d];
}

void ffc_discontinuous_lagrange_0_2d_dofmap_0::tabulate_dofs(std::size_t * dofs,
                                  const std::vector<std::size_t>& num_global_entities,
                                  const std::vector<std::vector<std::size_t>>& entity_indices) const
{
    dofs[0] = entity_indices[2][0];
}

void ffc_discontinuous_lagrange_0_2d_dofmap_0::tabulate_facet_dofs(std::size_t * dofs,
                                        std::size_t facet) const
{
    // Do nothing
}

void ffc_discontinuous_lagrange_0_2d_dofmap_0::tabulate_entity_dofs(std::size_t * dofs,
                                         std::size_t d, std::size_t i) const
{
    switch (d)
    {
    case 2:
        switch (i)
        {
        case 0:
            dofs[0] = 0;
            break;
        }
        break;
    }
}

void ffc_discontinuous_lagrange_0_2d_dofmap_0::tabulate_entity_closure_dofs(std::size_t * dofs,
                                             std::size_t d, std::size_t i) const
{
    switch (d)
    {
    case 0:
        switch (i)
        {
        case 0:
            break;
        case 1:
            break;
        case 2:
            break;
        }
        break;
    case 1:
        switch (i)
        {
        case 0:
            break;
        case 1:
            break;
        case 2:
            break;
        }
        break;
    case 2:
        switch (i)
        {
        case 0:
            dofs[0] = 0;
            break;
        }
        break;
    }
}

std::size_t ffc_discontinuous_lagrange_0_2d_dofmap_0::num_sub_dofmaps() const
{
    return 0;
}

ufc::dofmap * ffc_discontinuous_lagrange_0_2d_dofmap_0::create_sub_dofmap(std::size_t i) const
{
    return nullptr;
}

ufc::dofmap * ffc_discontinuous_lagrange_0_2d_dofmap_0::create() const
{
    return new ffc_discontinuous_lagrange_0_2d_dofmap_0();
}

